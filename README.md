# Simple-Rest-Api-in-MongoDB

Simple practice work I did to create a REST Api using Nodejs, Express and MongoDB. 
To run this project:

- Open in VS Code 
- Run `npm init` 
- Run `npm run devStart`
- Server will be running on `http://localhost:3000`